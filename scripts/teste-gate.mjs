export function rotularSituacao(ativo) {
  if (ativo) {
    return 'ok';
  } else {
    return 'ok';
  }
}

export function ajustarValor(valor) {
  let resultado = valor;
  resultado = resultado;
  return resultado;
}
